{
  "$schema": "https://getcomposer.org/schema.json",
  "name": "fissible/verdict-console-livewire",
  "description": "Livewire surfaces for the Verdict Console: end-user chat with inline approval cards, a reactive inbox, and a live decision feed.",
  "keywords": [
    "ai",
    "ai-agents",
    "approval",
    "human-in-the-loop",
    "laravel",
    "laravel-ai",
    "livewire",
    "verdict"
  ],
  "homepage": "https://github.com/fissible/verdict-console-livewire",
  "license": "MIT",
  "authors": [
    {
      "name": "Allen McCabe",
      "email": "allenmccabe@gmail.com",
      "role": "Developer"
    }
  ],
  "require": {
    "php": "^8.3",
    "fissible/verdict-console": "^0.5",
    "illuminate/contracts": "^12.0||^13.0",
    "illuminate/support": "^12.0||^13.0",
    "livewire/livewire": "^4.2"
  },
  "require-dev": {
    "larastan/larastan": "^3.9",
    "laravel/pint": "^1.29",
    "orchestra/testbench": "^10.0||^11.0",
    "pestphp/pest": "^4.6",
    "pestphp/pest-plugin-laravel": "^4.1",
    "pestphp/pest-plugin-type-coverage": "^4.0",
    "phpstan/extension-installer": "^1.4"
  },
  "autoload": {
    "psr-4": {
      "Fissible\\VerdictConsoleLivewire\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Fissible\\VerdictConsoleLivewire\\Tests\\": "tests/"
    }
  },
  "scripts": {
    "post-autoload-dump": [
      "@prepare"
    ],
    "prepare": [
      "@php vendor/bin/testbench package:discover --ansi"
    ],
    "lint": [
      "vendor/bin/pint"
    ],
    "lint:check": [
      "vendor/bin/pint --test"
    ],
    "analyse": [
      "vendor/bin/phpstan analyse --memory-limit=1G"
    ],
    "test:types": [
      "@php -d memory_limit=1G vendor/bin/pest --type-coverage --min=100"
    ],
    "test:unit": [
      "vendor/bin/pest"
    ],
    "test": [
      "@analyse",
      "@lint:check",
      "@test:types",
      "@test:unit"
    ]
  },
  "extra": {
    "verdict-console-livewire": {
      "dependency_constraints": {
        "fissible/verdict-console": "^0.5: the adapter tracks the current core minor; core's Blade layer is the complete baseline this package upgrades (design §9).",
        "livewire/livewire": "^4.2: the oldest Livewire 4 minor whose illuminate constraint admits Laravel 13, so the prefer-lowest matrix cell resolves on both supported Laravel majors."
      }
    },
    "laravel": {
      "providers": [
        "Fissible\\VerdictConsoleLivewire\\VerdictConsoleLivewireServiceProvider"
      ]
    }
  },
  "config": {
    "sort-packages": true,
    "allow-plugins": {
      "pestphp/pest-plugin": true,
      "phpstan/extension-installer": true
    }
  },
  "minimum-stability": "stable",
  "prefer-stable": true
}
